[workspace]

[package]
name = "example-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk = { path = "../../plugin-sdk", features = ["guest"] }
serde = { version = "1.0", features = ["derive"] }
extism-pdk = "1.4.1"
